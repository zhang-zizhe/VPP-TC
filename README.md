1. In your python environment (virtual environment recommanded), run:
```bash
pip install -r requirements.txt
```
2. Install pytorch with cuda compatible with your cuda version manually.
3. Go to `scripts` and run:
```bash
python main.py --constraints=both --search=bisection 
```

